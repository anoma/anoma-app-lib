module Token.Tests.AnomaNode.Step1_InitializeDummy;

import Stdlib.Prelude open;
import Data.Set as Set open using {Set};
import Anoma open;

import Token.Tests.AnomaNode.Step0_Setup open;
import KeyValueStorage.Indexing open;
import Utils.Dummy.Transaction as Dummy open;

-- Create a dummy resource for the ;Fixed; supply token.
tx : Transaction :=
  Dummy.initialize@{
    self := alice;
    dummy
  };

as : List (NatKeyValue.Assignment (Set Commitment)) :=
  prepareAssignments@{
    tx
  };

main : Transaction := tx;
